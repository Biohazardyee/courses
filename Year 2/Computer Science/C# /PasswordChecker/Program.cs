using System;

namespace PasswordChecker
{
    class Program
    {
            // ask user to enter password
            // ask user to enter password again
            // check if they are not empty
            // if so check if they are the same
            // if so print they are the same
            //if not print they do not match
            // if empty print please enter a password
        static void Main(string[] args)
        {
            
        }
    }
}